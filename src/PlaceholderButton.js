import React, {useState, useEffect} from "react"

export default function PlaceholderButton() {

    return (
        <section className={"countdown"}>
            <h1>Minting avaliable NOW!</h1>
        </section>
    )
}